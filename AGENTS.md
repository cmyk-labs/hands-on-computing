# 项目协作协议

本文件适用于整个项目。用户最新明确要求优先于项目文档；Claude Code 通过根目录的 `CLAUDE.md` 读取本协议。

## 规则归属与适用范围

修改前读取本文件，以及从项目根目录到目标文件所在目录沿途适用的 `AGENTS.md`。教学 Notebook 另读 [Notebook 编写协议](docs/notebook-protocol.md)，代码按实际语言参考下列编程规范。位于 `drafts/` 的课程草稿按拟归属课程读取补充协议，不能因尚未移入正式目录而跳过课程要求。

| 文档 | 唯一维护职责 |
| --- | --- |
| 根 `AGENTS.md` | 全项目协作、教学与代码原则、来源要求、文件归属和检查范围 |
| `docs/notebook-protocol.md` | Notebook 单元结构、排版、资源标注、图示制作与执行方法 |
| 课程 `AGENTS.md` | 该技术专属的讲解边界、展示方式和检查方法 |
| 课程 `README.md` | 实际环境版本、安装、工作目录、运行与当前验证限制 |
| 课程 `plan.md` | 学习目标、知识范围、章节安排、理论或标准基线及实施状态 |
| 编程规范 | 可独立阅读的语言、平台规则与一般工程建议 |
| `docs/START_HERE.md` | 通用阅读入口、Notebook 阅读工具和反馈方式 |
| `docs/project-plan.md` | 项目方向、交付顺序与当前进度摘要 |
| `paths/README.md` | 正式课程与项目导航 |

全局要求适用于现有和后续课程。课程协议只维护技术差异，不能放宽来源、真实性和最简代码要求。全局明确允许的展示或资源特例在课程协议中写明适用对象；不能笼统宣称“局部规则优先”。一般工程建议不自动成为每个教学示例的实现清单，也不能改变语言与 API 的实际行为。

同一规则只维护一份完整说明；其他文档用链接指向该说明，不复制另一份近似规则。课程规划不授予开展新任务的权限，历史进度和已确认样章也不等于后续任务授权。

## 项目目标与目录约束

`hands-on-computing`（《动手学计算机》）是一套从编程基础逐步走向 AI 工程的实践教程，以“理解原理，动手实践”为教学理念。正文以中文为主，知识解释有依据，实践可复现，运行结果真实可核查。

| 目录 | 职责 |
| --- | --- |
| `content/` | 课程正文与确有需要的随章示例、实验和资源 |
| `paths/` | 一个总目录索引，只导航，不复制正文 |
| `projects/` | 经任务明确的综合项目及代码、运行说明和验证记录 |
| `docs/` | 通用使用说明、Notebook 协议与项目方案 |
| `drafts/` | 本地草稿和临时作者工具，由 `.gitignore` 忽略，不提交 Git |

- 根目录说明文件保留 `README.md`、`README.zh-CN.md`、`AGENTS.md`、`CLAUDE.md` 和 `LICENSE`；其他项目说明归 `docs/`。不要新增资料库、日志、规范、模板或脚本等顶层目录。
- 草稿不进入正式索引，正式文档不链接草稿，不强制添加草稿到 Git。草稿经来源核查与必要运行检查后，按任务范围整理到正式目录；临时作者工具和产物用后清理，用户要求保留的草稿除外。
- `content/` 的一级分类以[总目录索引](paths/README.md)为准，各分类 `README.md` 用一句话说明范围。AI 原理与应用工程统一归入 `content/AI原理与应用/`。跨领域内容选择一个主要归属，不复制正文。
- 新增课程，包括仅编写规划，先在知识分类下建立独立技术或专题目录，再在其中维护 `plan.md`。不在分类根目录放课程规划或依赖清单，不为任务之外的技术创建占位课程。
- 新增技术目录采用官方标准名称和大小写；无官方名称的专题及普通文件默认使用英文小写 `kebab-case`。现有名称不因本规则自动更名。可导入 Python 模块与包用 `snake_case`，惯例文件名保持原样。
- Notebook 采用“二位章节序号-规划中的章节名称.ipynb”，直接放在技术目录，正文唯一一级标题与章节名称一致。不按术语数量机械拆章，不为单章增加重复目录层级。
- 两份主页保持内容对应和语言切换链接。课程简介用一句话按学习顺序概括基础、核心、进阶与综合实践；对照规划与正文核实范围，不把待编写或仅有入口的内容写成已经讲解或运行。分类顺序为“编程语言 → AI 原理与应用 → Web 与应用开发”；只改顺序时整体移动表格，不顺便改写简介。

## 课程编写的七条原则

以下为全局教学原则，从课程规划、样章到正文与随章代码统一执行。代码取舍适用于本项目编写或修改的全部代码，包括 Notebook、Markdown 示例、编程规范示例、配套脚本、共享模块、测试、综合项目、草稿和作者辅助工具；文件位置及“工程代码”身份不构成豁免。

1. **一个知识点优先一组说明与代码。** 用一个 Markdown 单元说明知识点、用途、核心规则和必要条件，紧接一个 Code 单元展示最小示例。简短的操作、参数与结果解释优先放在相邻代码注释中；定义、必要推导、概念关系和需要展开的结果分析使用相邻 Markdown，避免重复。知识点按要理解的机制或解决的问题划分，用途或规则不同的内容可以分别展开；不以一个 API 名称或固定单元数决定粒度。复杂知识点按实际学习步骤拆分，不强行合成长代码。纯理论、原生脚本、浏览器及逐类绘图的展示方式见 Notebook 协议及明确的课程特例。
2. **注释说明读者需要理解的内容。** 默认使用简洁中文；长代码解释关键步骤，复杂短代码补充必要说明，重点交代用途、参数、形状、单位、状态与原因，不逐行翻译语法。相关注释、docstring 与代码同步维护，不删除已有清楚且仍然适用的说明。
3. **每处观察输出写明预期。** 文本、表格、图形、页面和交互的预期都写在相邻代码注释中。固定结果说明具体值、类型或顺序；表格说明关键行列、形状与缺失特征；图形和页面说明应看见的内容及操作后的变化。连续输出可以合用注释，但逐项对应，不只写“输出结果”，不复制整张表。
4. **固定结果与运行差异分开。** 随机样本、耗时、临时路径、进程号和请求标识说明稳定特征、合理范围或字段关系；实测值注明“本次”。浮点结果说明必要的误差量级与容差，样本量与理论量分开，不承诺每次输出相同或跨环境逐位一致。
5. **名称简短明确，示例从小到大。** 不为缩短使用难懂缩写，也不堆叠冗长前后缀；保留必要单位、公式对应和公开接口名称。先展示最小可理解操作，再增加当前知识点所需内容，不预建类、配置层、服务管理器或未讲解的包装。
6. **默认输入合法，只保留真实逻辑。** 所有代码默认输入合法、环境满足已说明的条件，直接执行操作。不为假设中的非法输入增加类型、范围、空值、字段或文件存在性检查，不机械增加 `main()`、入口保护和异常包装；发生异常时让原始异常暴露。只有当前功能确实存在不同处理情况，例如分类计算、筛选、算法停止或状态变化，才使用必要分支。条件判断、异常处理、校验或入口机制本身是当前教学目标时，展示该目标所需代码。实际恢复、错误转换及资源关闭也须有当前任务中的具体用途，不能仅凭“接口契约”“外部输入”或“工程实践”增加防护。测试与验收保留核对当前目标所需的断言和预期失败检查，失败必须如实报告。不得用三元表达式、布尔短路、默认值、异常抑制或批量忽略警告变相兜底。判断依据是：合法输入下，该处理是否仍为当前功能、教学或检查目标所必需。
7. **教学目的与运行结果真实。** 预测练习不提前在待预测语句旁给答案，只提示运行后的核对维度；错误示例写明原因和预期异常。正文片段、配套源码、注释与真实输出保持一致。不编造引用、截图、跑分、执行计数或验收结果，不把静态检查、已有输出核对写成实际重跑。

## 教学组织与独立运行

- 以章节的明确学习目标决定范围；按已确定规划覆盖必讲内容与必要边界，选学显式标记，不成为后续主线的隐藏前提。“全面”不等于穷举全部 API。
- 教学精简以读者能独立理解当前知识点为前提，删除重复和无关内容，保留必要定义、条件、规则及结果解释。只列名称、调用代码或展示输出不视为讲解完成；不把减少文字或单元数量作为目标。
- 按当前学习目标检查读者能否解释规则、预测示例结果、说明适用条件，并完成一次条件变化或方法选择。公式保留必要推理，算法连接问题、计算和结果判断，API 说明关键参数与返回含义，复杂类型说明具体展开或推断关系；不强制每节套用相同标签。
- 每章独立提供导入、输入和运行条件，不依赖其他 Notebook 的变量、历史内核状态或运行产物。“前置知识：”只列具体概念或操作，不写“先修第 X 章”，不介绍其他课程完成状态。可提供非必需的补充阅读链接，但不能以链接代替本章必要说明。
- 输入尽量就地提供；同一任务中已讲清的短流程可以复用变量，注明来源，不让无关知识点依赖贯穿全章的可变状态。综合应用组合已讲内容，不提前承载整套工程。
- 对比示例尽量保持其他条件相同，只改变当前观察因素。随机实验记录种子及影响复现的条件；性能实验先核对结果，再测有限规模，不预填跑分。
- 通用读取、评估或绘图流程首次在正文展开；确有复用时才抽取模块，后续说明用途、输入输出和可配置条件。当前讲解的核心逻辑始终在本章展开。导入模块不自动训练、下载或启动服务；状态由当前章显式准备。课程可以要求更强的代码自包含。
- 练习提供任务、必要输入、可观察标准和适当提示，不用未定义变量或无条件报错占位。可结合操作、预测、条件改变与方法选择；各章至少安排一道需要改变条件或解释选择的题目，不能只换数字重复调用。重点题提供分层提示与独立解析，预测题保持思考空间。
- 对照与练习明确改变的条件和保持不变的因素；解析直接回答题干，并核对所讨论变体的实际行为。不能在解析中悄悄增删条件，或用另一个可运行方案替代对原问题的解释。
- 讲解用短段落和必要条目，先说明用途与行为，再引入术语；易歧义术语首次中英对照，库名、API 和工具名保留原文。符号首次出现说明含义及必要的范围、类型、单位或维度，保留辨认语法所需的操作数。具体排版见 Notebook 协议。

## 权威来源与引用

- 所有教学知识点及代码注释中的知识解释，须基于写作前核查的官方或第一方来源：官方文档、正式标准、原始论文、官方源码、作者教材或维护者说明。来源应直接支持结论及适用条件，不能凭记忆、推测或实测补写知识解释。
- 定位到页面、小节、版本、论文页码或源码位置。学习组织、原创输入和课程安排可以由项目设计，但不能冒称官方结论。来源入口和规划映射不替代逐点核查。
- 核查具体定位确实存在，并直接支持所写陈述及其条件；官方域名、页面可访问或总览列出 API 均不能单独证明细节。跨库或版本比较分别核查各端依据；使用 stable、latest 等动态入口时核对实际版本，标明与实测版本有关的差异。必要推导明确步骤，实测观察不得冒充通用结论。
- Notebook 来源统一在篇末汇总，正文知识点后不就近放参考链接；站点分组、定位与表格格式见 Notebook 协议。独立 Markdown 编程规范按自身文档结构保留来源定位。
- 摘录、节译和改编明确标识，忠实保留来源含义、强度和条件，遵守授权，不整篇搬运。来源无法核实时不写入正式知识正文，在交付中说明缺口。
- 不要求下载或另存原始资料，不增加来源档案、资料索引或引用日志。实际观察与知识结论分开，运行结果不能替代权威依据。

## Jupyter Notebook 内容规范

Notebook 为每章唯一教学正文，先写正文和最小示例，再按需要补充资源。结构、代码展示、开篇、引用、图示与预期异常执行方法统一见 [Notebook 编写协议](docs/notebook-protocol.md)，课程协议只维护其明确允许的技术特例。

- 只有独立服务、多文件组织、浏览器资源或已讲流程的实质复用需要时，才添加最小配套文件；仅供作者制作或检查的工具不作为课程配套交付。
- 章节源码与运行所需资源默认放 `scripts/二位序号-英文主题/`。共享的可导入模块按课程约定放 `scripts/` 下合法模块或包路径；不将含连字符的章节目录用作 Python 包名。
- 单独管理的教学数据默认放课程 `data/`，头部另列“数据：”；脚本列在“配套脚本：”，没有相应文件则省略，不写“无外部脚本”来承载数据清单。NumPy、pandas 既有 `date/` 在课程协议中作为存量例外保留；更名须作为独立任务同步代码、命令、链接并重新验证，不仅修改文档路径。
- 辅助学习示意图统一放 `image/illustration/`，真实截图与需另存的实际成图放 `image/`；训练用图像属于数据。图源选择、命名、许可、字体和实际渲染检查见 [图示要求](docs/notebook-protocol.md#图示的制作与整合)，Notebook 图形输出不要求逐张另存。
- 项目内固定路径使用完整相对路径字符串；存在变量时只连接完整固定后缀，不逐级拆分固定目录名。说明相对于 Notebook、工作目录、模块或页面 URL 的起点；移动后同步核对全部引用。

## 编程规范

编程规范保持面向读者、可独立阅读的职责；语言与平台事实、风格选择、工程建议分别表述。其示例同样受本项目最简代码原则约束；介绍校验、异常等机制时按明确教学目标展示，不将工程建议扩写为所有课程必须实现的检查。

| 技术 | 规范 |
| --- | --- |
| Python | [Python 编程规范](content/编程语言/python/Python编程规范.md) |
| JavaScript | [JavaScript 编程规范](content/编程语言/javascript/JavaScript编程规范.md) |
| TypeScript | [TypeScript 编程规范](content/编程语言/typescript/TypeScript编程规范.md) |
| HTML | [HTML 编程规范](content/Web与应用开发/html/HTML编程规范.md) |
| CSS | [CSS 编程规范](content/Web与应用开发/css/CSS编程规范.md) |
| FastAPI | [FastAPI 编程规范](content/Web与应用开发/FastAPI/FastAPI编程规范.md) |

不把课程路径、中文注释、章节编号、检查命令和教学单元组织写成通用语言规范。既有代码按实际修改范围整理，不因规则调整批量重排引号、行宽或工程结构。

## 环境与依赖

- 实际工具版本、安装步骤和日常运行入口集中在课程 `README.md`；`plan.md` 只维护理论或标准基线，章节保留影响行为的版本条件。Notebook 阅读工具与示例运行环境分别说明。
- pip 依赖使用课程 `requirements.txt`；npm 依赖使用课程 `package.json` 和锁文件。没有实际依赖则不建占位清单。多包、打包和分发本身是教学目标时，允许必要的包元数据，不为每章复制一套课程依赖或独立环境。
- Python 课程及现有辅助脚本沿用约定的 Conda 环境；特殊版本实验按章节条件执行。不得擅自另建 uv/venv、切换环境、换源或升级无关依赖；安装失败与冲突如实报告。干净环境复验按明确任务范围执行。
- 依赖、版本或运行命令改变时，同步配置、README 和受影响章节。共享环境中同一依赖发生变化时，核对所有使用该依赖的课程及受影响实验。非 Python 工具不列为 pip 包。
- 安装、配置和运行采用“Step 1：一句话说明”紧接一个命令块，一步只做一件事；界面操作直接写操作。环境管理、编译和打包本身属于教学目标时，其讲解仍留在对应章节。

## 协作与验收

- 保留用户已有工作，只执行已授权范围。新增大专题先说明目标、归属、前置知识和成本。用户要求先审报告时，仅生成报告与草案，确认后再落地正式文件。
- 新课程或明显改变教学形式时，先形成一个代表性样章；是否需要用户逐章确认以当前任务约定为准。已有样章与授权可继续使用，不把每章编写或日常修订默认变成重复审批。
- 如任务采用并行编写，同一 Notebook 只由一名编写者修改，共享计划、环境、依赖和索引由协调者维护。此为分工约定，不构成自动开展并行任务或扩大课程范围的授权。
- 正文变动时核对导航；新增、删除、改名、移动或范围变化时更新总目录及受影响入口。只修改错字、注释且导航不变时，不制造无意义索引改动。
- 新增或改变执行行为、依赖、输入、运行条件的 Notebook，从空内核顺序运行全部 Code 单元并保存真实输出；共享模块变化检查全部受影响调用。纯说明或注释变更按 Notebook 协议核对已有输出及必要刷新范围，不宣称重新运行。
- 原生脚本、编译项目和浏览器示例按其真实运行方式检查；混合章节分别验收内核、编译、浏览器等部分。语法通过、资源返回成功或生成截图不等于交互通过。
- 有实验时说明工作目录、环境、命令、输入、成功标准、资源成本及必要关闭方法。实际创建的文件、连接、服务和进程及时释放；不把关闭客户端当作关闭服务器。
- 每次交付区分安装、静态检查、已有输出核对和实际运行，说明未执行或失败范围。Notebook 正文不增加验证记录、核对内核或专门打印环境信息的单元。
- 教学验收与执行验收分别完成：对照学习目标检查概念、规则、条件、结果解释和练习是否形成完整学习过程，再核对真实运行。运行通过或条目出现不能代替讲解完整性；发现缺口按实际内容补充，不新增形式化记录文件。
- 课程 README 保留影响读者复现的当前验证版本、日期、覆盖范围和限制；项目方案只保留进度摘要，详细过程放当次交付，不另建 Notebook 验证日志。独立综合项目可按项目方案保留简短、可追溯的验证记录。
- 不提交密钥、模型权重、大型数据、缓存和完整服务日志。模拟数据明确标识；涉及下载、账号、GPU 或付费调用时按已授权任务范围执行并说明条件。
- 修改协议后同步核对根入口、Notebook 协议和受影响课程文档。文档已修改不等于既有正文全部满足新规则；逐章改动按实际影响验证，不伪造全课程验收。
- 原创教程与代码采用 [CC BY-NC-SA 4.0](LICENSE)，署名 CMYK Labs（cmyk-labs）；第三方材料遵守各自授权，不擅自变更许可。
