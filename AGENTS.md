# 项目协作协议

本文件适用于整个项目，是所有贡献者和 AI 助手共同遵循的项目规则。Claude Code 通过根目录的 `CLAUDE.md` 读取本协议。用户最新明确要求优先于项目文档。

处理子目录文件前，还须读取从项目根目录到目标文件所在目录沿途适用的 `AGENTS.md`。技术专属的写法与检查要求集中在对应目录的补充协议中，通用文档不重复维护；补充协议只细化其适用范围内的要求。

## 项目目标

`hands-on-computing`（《动手学计算机》）是一套从编程基础逐步走向 AI 工程的计算机实践教程，以“理解原理，动手实践”为教学理念。正文以中文为主，知识解释有权威依据，实践可复现，实验记录真实可核查。

## 目录约束

保留四个正式目录和一个本地草稿目录：

| 目录 | 职责 |
| --- | --- |
| `content/` | 正文及随章示例、实验 |
| `paths/` | 一个总目录索引，链接到已有正文和项目，可标明阅读顺序 |
| `projects/` | 综合实践项目及其代码、运行说明和验证记录 |
| `docs/` | 开始使用、Notebook 编写协议、项目方案（含路线图） |
| `drafts/` | 本地草稿，由根目录 `.gitignore` 忽略，不提交 Git |

草稿不进入正式目录索引，不从正式文档链接到草稿文件。草稿转为正式内容时，先满足引用与验证要求，再移入对应正式目录并更新索引。不得强制添加 `drafts/` 中的文件到 Git。

根目录的说明文件保留英文主页 `README.md`、中文主页 `README.zh-CN.md`、`AGENTS.md`、`CLAUDE.md` 和许可声明 `LICENSE`；两份主页保持内容对应，并提供语言切换链接。其他项目级说明放在 `docs/`，各技术的编程规范放在 `content/` 对应技术目录。课程正文仍放 `content/`，目录索引仍放 `paths/`，不将它们或引用的原始资料搬入 `docs/`。不要自行增加资料库、日志、规范、模板、脚本等顶层目录。不提前创建具体技术栈目录或占位课程，按实际任务添加。

正文只保留一份，目录索引只负责导航，不复制正文。目录存在不代表内容已完成。

`content/` 的中文一级知识分类以[总目录索引](paths/README.md)为准，每个分类用 `README.md` 中的一句话说明收录范围。AI 基础原理与应用工程统一归入 `content/AI原理与应用/`。后续按“知识领域 → 具体技术或专题 → 主题章节”组织。一级分类不放 `requirements.txt`，依赖清单在具体技术目录建立时添加。跨领域知识选择一个主要归属，其他位置引用，不复制正文。

文档按职责维护一份完整说明：本文件保存协作规则，[开始使用](docs/START_HERE.md)保存阅读入口、通用 Notebook 工具与反馈方式；技术目录的 `README.md` 保存环境配置与运行步骤，`AGENTS.md` 保存教学组织、检查要求与课程约定，编程规范提供可独立阅读的通用代码风格与工程建议；[项目方案](docs/project-plan.md)保存长期规划和当前进度，Notebook 协议保存通用写作要求。`CLAUDE.md` 引用本文件，其他入口只做导航或简要提醒。

所有说明力求直观简洁。安装、配置和运行操作采用“Step 1：一句话说明”紧接一个命令块；每步只做一件事，界面操作直接写操作。其他讲解使用短段落或必要条目，保留前提和易错边界，删除重复解释。

## 权威来源与引用

- 所有教学知识点都必须基于写作前已核查的官方或第一方来源；官方文档、正式标准、原始论文、官方源码及维护者说明都可以作为一手依据。
- 写作前核查来源是否直接支持相应知识点及其适用条件。不能凭 AI 记忆、自行理解、推测或编造补写知识解释。
- 写作时必须定位到直接支持知识点的具体官方或第一方页面、小节、版本、论文页码或源码位置；Notebook 正文不在知识点后就近放链接，统一在篇末“参考与引用来源”汇总并保留必要定位。不能用不相关的链接充当依据。
- 不要求下载、复制或另存原始资料，不要求建立来源归档目录、单独的资料索引或引用记录文件。
- 使用原文摘录时明确标识，翻译忠实于来源，不添加个人解释；引用遵守来源授权，不整篇搬运。
- 来源无法核实时，暂不写入正式知识正文，在本次交付说明中指出缺口即可，不强制另建文件。
- 禁止编造引用、跑分、截图、实验结果和执行状态。实际观察与知识结论分开，实测不能替代知识点的权威依据。

## 编程规范

下列规范是面向读者、可脱离课程独立阅读的文档，依据官方规范或第一方风格指南编写。编写或修改代码时按实际语言参考，包括其他课程中的辅助脚本；每种技术只维护一份，技术目录的 `AGENTS.md` 引用对应文件。

| 技术 | 规范 |
| --- | --- |
| Python | [Python 编程规范](content/编程语言/python/Python编程规范.md) |
| JavaScript | [JavaScript 编程规范](content/编程语言/javascript/JavaScript编程规范.md) |
| TypeScript | [TypeScript 编程规范](content/编程语言/typescript/TypeScript编程规范.md) |
| HTML | [HTML 编程规范](content/Web与应用开发/html/HTML编程规范.md) |
| CSS | [CSS 编程规范](content/Web与应用开发/css/CSS编程规范.md) |

- 规范正文保留来源的规则含义与强度，区分语言或平台约束、风格要求和可选建议；删节、翻译或补充须明确标识，并遵守来源授权。
- 不将现有课程的引号、路径、中文注释、章节编号或检查命令写成通用规范。课程专属要求留在 `AGENTS.md`，环境与运行入口留在 `README.md`。
- 教学中的替代写法、机制演示和反例须说明目的及适用条件；最小入门示例不机械套用工程包装。
- 代码规范不替代 Notebook 教学协议；环境版本和安装命令集中在技术目录 `README.md`。现有代码按实际修改范围逐步整理，不因规范更新批量重排课程。

## Jupyter Notebook 内容规范

编写、修改或验收任何教学 Notebook（包括草稿）前，必须读取并遵守 [Jupyter Notebook 编写协议](docs/notebook-protocol.md)及适用的技术目录补充协议。本文件只保留入口与关键约束，不重复维护模板。

- 每章围绕一个明确主题，使用一份 `.ipynb` 作为唯一教学正文；相关知识点可以合讲，章节数量按学习关系决定，不把每个术语机械拆成一篇。正文按“知识点讲解 → 代码”组织，结果说明写进代码注释；具体展示与运行方式遵循对应技术目录的补充规则。
- 头部单列“学习目标：”和“前置知识：”，分别简述本章学习收获与所需概念或操作，不用“先修第 X～Y 章”代替前置知识；完整开篇要求见 Notebook 编写协议。
- 头部环境入口链接到所属技术目录的 `README.md`；完整安装步骤集中维护，章节保留自身版本条件、工作目录和运行命令。环境管理、编译与打包等教学内容仍在对应章节讲解。
- 必需的配套脚本及资源放在所属技术目录的 `scripts/章节/` 下，头部写明“配套脚本：”的位置并简列用途；按需创建，及时清理临时文件。命名、链接和执行要求见 Notebook 编写协议。
- 项目内固定路径使用完整相对路径字符串；存在变量时只连接完整固定后缀，不逐级拆分目录名称。课程路径写法与起点说明见对应技术目录 AGENTS.md，教学路径与链接见 Notebook 编写协议。
- 教学讲解简明具体，按语义分成短段落，必要术语就地解释；采用通行专业译法，易歧义者首次中英对照，库名、API 和工具名称保留原文。删除重复说明，保留必要条件和易错边界。
- 分类交叉或跨章节出现的概念如容易混淆，用一句话注明关系或本章侧重点，例如 `list` 既是内置类型，也属于容器。
- 类型、术语或 API 总览表保留原文名称，增加“中文名称／含义”列，独立项目逐行对应，例如 `set` 对应“集合”。不要把多个需要分别理解的名称挤在同一行；用途、可变性等按主题补充。
- 写法或公式中的占位符在首次出现处说明含义，必要时交代取值范围、类型、单位或维度。保留有助于辨认语法的操作数，例如 `+x`、`-x`，并说明 `x` 代表参与运算的数值，不能让读者猜测。完整措辞与排版要求以 Notebook 编写协议为准。
- 每个知识点写作前核查能直接支持其内容的官方或第一方来源；正文不就近放参考链接，篇末最后一个 Markdown 单元统一为“参考与引用来源”，同一网站只列一次，合并其支持的多个知识点并保留必要定位。
- 遵守协议中的章节结构、本章小结和练习要求；Code 单元从空内核顺序执行并保存真实输出，浏览器示例检查实际页面与交互。Notebook 不编写“验证记录”和“核对内核”部分，不添加专门打印解释器路径或环境信息的代码单元；实际检查结果在当次交付说明中报告。
- 各语言代码参考对应编程规范，具体课程约定以适用的 `AGENTS.md` 为准；教学组织遵循 Notebook 协议和技术目录补充协议。
- 修改 Notebook 协议后同步核对本入口及相关文档；不得将不符合协议的草稿标记为已验收。

## 环境与依赖

- 各技术目录在 `README.md` 中说明实际工具版本、安装步骤、工作目录与运行入口；首次准备和日常运行按需分开。完整版本清单集中维护，章节保留影响结果的版本条件。
- 使用对应技术的依赖格式：有实际 pip 依赖时维护 `requirements.txt`；需要 npm 依赖时维护 `package.json` 和锁文件。没有相应依赖就不创建清单、不写安装命令，不保留只有注释的占位文件。
- Notebook 阅读工具与示例运行环境分别说明。通用阅读工具见开始使用；课程所需的内核和依赖由技术目录维护。Python 标准库辅助服务只需说明 Python 运行条件，不因此创建 pip 清单。
- Python 课程和现有 Python 辅助脚本沿用已约定的 Conda 环境；具体版本与步骤见对应技术目录说明，特殊版本实验按章节条件执行。不得擅自切换环境、另建 uv/venv 环境、换源或升级无关依赖；安装失败或冲突时如实报告。
- 新增依赖、改变版本或运行命令时，同步更新依赖配置、技术目录 `README.md` 和受影响章节，并检查相应入口。非 Python 工具不写成 pip 包。

## 协作与验收

- 修改前阅读相关文件，保留用户已有工作，只执行当前任务范围。
- `content/` 的一级分类使用已确认的中文名称；根目录名称保持现有英文，具体技术目录和普通文件默认使用英文小写 `kebab-case`。教学 Notebook 使用“二位章节序号-规划表中的章节名称.ipynb”，例如 `01-基础语法.ipynb`，正文一级标题与章节名称一致。可导入的 Python 模块和包使用 `snake_case`，`README.md`、`AGENTS.md`、`CLAUDE.md` 等保留惯例。
- 已按章节规划的 Notebook 直接放在对应技术目录，不再为单章增加重复的分类层级；目录层级有实际需要时再调整。
- 正文变动时更新总目录索引，确保链接指向实际存在的内容。
- 有实验时说明工作目录、环境版本、命令、成功标准和实际结果；需要时提供关闭和清理步骤。执行与浏览器检查按 Notebook 编写协议进行。
- 安装成功、语法检查通过和实验运行通过分别报告；未执行的检查明确写明，依赖或代码变化后不能沿用旧结果宣称通过。
- 不提交密钥、模型权重、大型数据、缓存和完整服务日志。付费调用等操作按具体任务范围执行。
- 一次改动围绕当前任务；新增大专题先说明学习目标、归属、先修要求、实验成本与现有内容的关系。交付时说明修改内容、实际检查结果及未验证事项，不将格式检查或文档构建通过等同于实验运行通过。不要为了简单文档工作额外建立流程或辅助目录。
- 原创教程与代码采用 [CC BY-NC-SA 4.0](LICENSE)，署名 CMYK Labs（cmyk-labs）；第三方材料的引用和使用遵守其各自授权，不擅自变更许可。
